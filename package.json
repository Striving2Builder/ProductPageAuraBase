{
  "name": "aurabase---ai-holistic-wellness",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate:blog-pages": "tsx scripts/generate-blog-pages.ts",
    "generate:sitemap": "tsx scripts/generate-sitemap.ts",
    "generate:rss": "tsx scripts/generate-rss.ts",
    "prepare:landing": "tsx scripts/prepare-landing.ts",
    "optimize:images": "tsx scripts/optimize-images.ts",
    "prerender:index": "tsx scripts/prerender-index.ts",
    "dev": "npm run generate:sitemap && npm run generate:rss && npm run generate:blog-pages && npm run prepare:landing && vite",
    "build": "npm run generate:sitemap && npm run generate:rss && npm run generate:blog-pages && npm run prepare:landing && npm run optimize:images && vite build && npm run prerender:index",
    "preview": "vite preview"
  },
  "dependencies": {
    "lucide-react": "0.462.0",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "autoprefixer": "^10.4.23",
    "postcss": "^8.5.6",
    "sharp": "^0.35.2",
    "tailwindcss": "^4.1.18",
    "tsx": "^4.19.4",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
